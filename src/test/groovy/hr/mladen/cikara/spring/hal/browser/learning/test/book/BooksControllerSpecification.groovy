package hr.mladen.cikara.spring.hal.browser.learning.test.book

import spock.lang.Specification

class BooksControllerSpecification extends Specification {
    // TODO: Add tests
    def "findAll should return list of books"() {
        given: "Book controller with mocked dependencies"
        when: "calling findAll"
        then: "list of books is returned"
    }
}
